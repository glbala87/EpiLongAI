apiVersion: v1
kind: Service
metadata:
  name: epilongai-api
  namespace: epilongai
  labels:
    app.kubernetes.io/name: epilongai
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: epilongai
    app.kubernetes.io/component: api
